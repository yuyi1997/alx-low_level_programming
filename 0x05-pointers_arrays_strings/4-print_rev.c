#include "main.h"

/**
 * print_rev -prints a string in reverse
 * @s: the string input
 */
void print_rev(char *s)
{
	int le
	while (s[len] != '\0')
		len++;
	while (len)
		_putchar(s[--len]);

	_putchar('\n');
}
